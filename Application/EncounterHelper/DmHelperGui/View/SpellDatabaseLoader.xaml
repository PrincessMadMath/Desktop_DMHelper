<UserControl x:Class="DmHelperGui.View.SpellDatabaseLoadder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="75" d:DesignWidth="300">
    <Grid>
        <StackPanel >
        <StackPanel HorizontalAlignment="Left"  Orientation="Horizontal"  Margin="0,0,0,10">
            <Button x:Name="BtnOpenSpellDatabase" Click="btnOpenSpellDatabase_Click" Content="Choose Spell Repo"/>
            <TextBlock x:Name="TxtSpellDatabase" Margin="10,0,0,0"/>
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="BtnLoadSpellDatabase" Click="btnLoadSpells_Click" Content="Load Spell Database"/>
            <TextBlock x:Name="TxtStatus" Margin="10,0,0,0"><Run Text="Not loaded"/></TextBlock>
        </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
